import React from 'react'
import Container from '../Container'
import styles from './footer.module.css'
const Footer = () => {
  return <Container classNames={styles.footer}>this is footer</Container>
}

export default Footer
