import React from 'react'
import Container from '../Container'
import styles from './header.module.css'
const Header = () => {
  return <Container classNames={styles.header}>this is header</Container>
}

export default Header
